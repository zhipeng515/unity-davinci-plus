# com.nexzones.davinci

The image loader. Drop-in compatible with
[shamsdev/davinci](https://github.com/shamsdev/davinci)'s fluent API, plus an
in-memory LRU decoded-texture cache (same-frame hits), preload, and
assignment-time loaded callbacks. See the [repo README](../../README.md).
